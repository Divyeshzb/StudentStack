# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByStatus_get for http method type GET in artillery framework
# RoostTestHash=9575439991
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_success.csv"
    fields:
      - "status"
    skipHeader: true

scenarios:
  - name: "Validate success responses for /v2/pet/findByStatus endpoint"
    flow:
      - get:
          url: "/v2/pet/findByStatus?status={{ status }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "category"
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
      - think: 1
