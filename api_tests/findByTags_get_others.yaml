# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByTags_get for http method type GET in artillery framework
# RoostTestHash=c791547528
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "findByTags_get_others.csv"
    fields:
      - "tag"
  plugins:
    expect: {}

scenarios:
  - name: "Test non-successful API scenarios"
    flow:
      - log: "1. Test with invalid URL"
      - get:
          url: "/v3/pet/findByTags"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "2. Test with invalid method type"
      - post:
          url: "/v2/pet/findByTags"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
      - think: 1

      - log: "3. Test with missing Authorization header"
      - get:
          url: "/v2/pet/findByTags?tags={{ tag }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "4. Test with invalid tag value"
      - get:
          url: "/v2/pet/findByTags?tags=invalidTag"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "5. Test with tag value not present in CSV"
      - get:
          url: "/v2/pet/findByTags?tags={{ missingTag }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1
