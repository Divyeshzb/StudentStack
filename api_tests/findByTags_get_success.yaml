# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/findByTags_get for http method type GET in artillery framework
# RoostTestHash=c791547528
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_success.csv"
    fields:
      - "tags"
    skipHeader: true
  http:
    timeout: 30
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - name: "Test Success API Responses for /v2/pet/findByTags"
    flow:
      # GET request to /v2/pet/findByTags endpoint
      - get:
          url: "/v2/pet/findByTags?tags={{ tags }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"
      - expect:
          - hasProperty: "category"
      - expect:
          - hasProperty: "name"
      - expect:
          - hasProperty: "photoUrls"
      - expect:
          - hasProperty: "tags"
      - expect:
          - hasProperty: "status"
      - think: 1
