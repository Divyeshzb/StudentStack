# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user/login_get for http method type GET in artillery framework
# RoostTestHash=474b7881ac
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "login_get_others.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true

scenarios:
  - name: "Non-Success API Requests"
    flow:
      # Invalid payload: Missing password
      - get:
          url: "/v2/user/login?username={{ username }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid payload: Missing username
      - get:
          url: "/v2/user/login?password={{ password }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid authentication: Incorrect token
      - get:
          url: "/v2/user/login?username={{ username }}&password={{ password }}"
          headers:
            Authorization: "Bearer incorrect_token"
          expect:
            - statusCode: 401
      - think: 1

      # Invalid URL
      - get:
          url: "/v2/user/logi?username={{ username }}&password={{ password }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
