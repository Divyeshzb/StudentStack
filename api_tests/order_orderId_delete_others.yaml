# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /store/order/{orderId}_delete for http method type DELETE in artillery framework
# RoostTestHash=6e4c4bbafe
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "order_orderId_delete_others.csv"
    fields:
      - "orderId"
    skipHeader: true

scenarios:
  - name: "Test non-successful API requests"
    flow:
      # Invalid orderId
      - delete:
          url: "/v2/store/order/invalidOrderId"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "code"
          think: 1

      # OrderId not found
      - delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - hasProperty: "code"
          think: 1

      # Unauthorized request
      - delete:
          url: "/v2/store/order/{{ orderId }}"
          headers:
            authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasProperty: "code"
          think: 1

      # Invalid URL
      - delete:
          url: "/invalid/url/{{ orderId }}"
          headers:
            authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
          think: 1
