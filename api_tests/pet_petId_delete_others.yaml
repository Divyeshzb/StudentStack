# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_delete for http method type DELETE in artillery framework
# RoostTestHash=fbd3e66b3b
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "petId"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Testing non-success API responses"
    flow:
      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "invalid_api_key"
          expect:
            - statusCode: 401
            - hasHeader: "WWW-Authenticate"
      - think: 1

      - post:
          url: "/v2/pet/"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - post:
          url: "/v2/invalid_url/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - post:
          url: "/v2/pet/{{ petId }}"
          headers:
            api_key: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalid_key: "invalid_value"
          expect:
            - statusCode: 400
      - think: 1
