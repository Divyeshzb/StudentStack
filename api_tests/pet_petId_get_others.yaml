# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}_get for http method type GET in artillery framework
# RoostTestHash=4dfd26d5a8
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_petId_get_others.csv"
    fields:
      - "petId"
    skipHeader: true

scenarios:
  - name: "Non-Success API Requests Test"
    flow:
      # Invalid petId
      - post:
          url: "/v2/pet/abc"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
      - think: 2

      # petId not found
      - get:
          url: "/v2/pet/0"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
      - think: 2

      # Unauthorized access
      - get:
          url: "/v2/pet/{{ petId }}"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
      - think: 2

      # Accessing invalid URL
      - get:
          url: "/v2/invalid_url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
      - think: 2
