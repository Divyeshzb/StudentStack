# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet_post for http method type POST in artillery framework
# RoostTestHash=11a919de45
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_post_others.csv"
    fields:
      - "id"
      - "name"
      - "photoUrls"
      - "status"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Test scenario for non-success API responses"
    flow:
      # Test case: Post request with missing fields in the payload
      - post:
          url: "/v2/pet"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "code"
      - think: 1

      # Test case: Post request with invalid token
      - post:
          url: "/v2/pet"
          headers:
            Authorization: "Bearer invalid_token"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401
      - think: 1

      # Test case: Post request with invalid URL
      - post:
          url: "/v2/invalid_url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            photoUrls: "{{ photoUrls }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404
      - think: 1
