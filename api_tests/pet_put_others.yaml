# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet_put for http method type PUT in artillery framework
# RoostTestHash=04bbbce6b4
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "pet_put_others.csv"
    fields:
      - "id"
      - "name"
      - "status"
    skipHeader: true
  headers:
    Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Test non-success cases for /v2/pet endpoint"
    flow:
      # Sending a PUT request with an invalid pet id
      - put:
          url: "/v2/pet"
          json:
            id: 99999999
            name: "{{ name }}"
            status: "{{ status }}"
          capture:
            - json: "$.message"
              as: "responseMessage"
          expect:
            - statusCode: 404
            - hasProperty: "message"

      - think: 1

      # Sending a PUT request without authorization token
      - put:
          url: "/v2/pet"
          headers:
            Authorization: ""
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 401

      - think: 1

      # Sending a PUT request with invalid payload
      - put:
          url: "/v2/pet"
          json:
            id: "{{ id }}"
            name: 123
            status: "{{ status }}"
          expect:
            - statusCode: 400

      - think: 1

      # Sending a PUT request to an incorrect URL
      - put:
          url: "/v2/wrongurl"
          json:
            id: "{{ id }}"
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 404

      - think: 1
