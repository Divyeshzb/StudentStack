# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /pet/{petId}/uploadImage_post for http method type POST in artillery framework
# RoostTestHash=354b987cbe
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
  payload:
    path: "uploadImage_post_others.csv"
    fields:
      - "petId"
      - "additionalMetadata"
      - "file"
    skipHeader: true

scenarios:
  - name: "Non-success API responses"
    flow:
      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "InvalidToken"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 401
      - think: 1

      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 400
      - think: 1

      - post:
          url: "/v2/pet/invalidId/uploadImage"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404
      - think: 1

      - post:
          url: "/v2/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "largeFile.jpg"
          expect:
            - statusCode: 413
      - think: 1

      - post:
          url: "/invalidUrl"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 404
      - think: 1
