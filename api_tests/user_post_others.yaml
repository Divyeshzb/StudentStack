# ********RoostGPT********
# Test generated by RoostGPT for test connector-api-spec using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /user_post for http method type POST in artillery framework
# RoostTestHash=fcaadbca4d
# 
# 
# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "user_post_others.csv"
    fields:
      - "username"
      - "firstName"
      - "lastName"
      - "email"
      - "password"
      - "phone"
      - "userStatus"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      http:
        timeout: 10

scenarios:
  - name: "Test scenario for non-successful API responses"
    flow:
      # Invalid payload - missing required fields
      - post:
          url: "/v2/user"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            phone: "{{ phone }}"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid payload - wrong data type
      - post:
          url: "/v2/user"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            username: "{{ username }}"
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            email: "{{ email }}"
            password: "{{ password }}"
            phone: "{{ phone }}"
            userStatus: "invalid"
          expect:
            - statusCode: 400
      - think: 1

      # Invalid URL
      - get:
          url: "/invalid-url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      # Test for unauthorized access
      - get:
          url: "/v2/user/{{ username }}"
          headers:
            Authorization: "Bearer InvalidToken"
          expect:
            - statusCode: 401
      - think: 1
